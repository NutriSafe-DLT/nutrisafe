# All scripts for the ordering service


### intializeOrderingService.sh
Starts an ordering ordering service including an ordering node with the modus solo. 

For executing you should be in the same directory.

